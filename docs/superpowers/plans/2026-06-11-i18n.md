# 前端 i18n 多语言改造 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `web/` 前端引入中英文 i18n 切换(react-i18next),默认跟随系统语言,用户切换后持久化到 localStorage,Sidebar 底部全局切换。

**Architecture:** react-i18next 多 namespace 资源(`common/nav` + 7 个页面 ns),`useLanguage` hook 复刻现有 `useTheme` 的 localStorage + 自定义事件同步范式,`LanguageSwitcher` 接入 `AppShell` footer。语言检测优先级:localStorage > `navigator.language`(zh→中文) > en。后端/宿主零改动。

**Tech Stack:** React 19 + TypeScript 6 + Vite 8 + i18next + react-i18next。

**关于测试(重要):** 本前端项目**无单元测试框架**(`package.json` 无 test script)。本计划不引入测试框架(YAGNI)。质量门槛改为:
- `npm run lint`(ESLint)
- `npm run build`(`tsc -b && vite build`,**含类型检查,是硬门槛**)
- i18next 的 `declare module` 类型扩展让 `t('ns:key')` 的 key 在**编译期**校验,拼错即 `tsc` 报错——这是本计划的核心防错机制
- 关键节点用 `npm run dev` 抽查中英文切换效果

---

## File Structure(创建/修改清单)

**新建:**
- `web/src/i18n/index.ts` — i18next 实例初始化 + 资源导入 + 语言检测 + 类型扩展
- `web/src/i18n/locales/en/{common,nav,home,prompts,transfer,devices,scratchpad,welcome,settings}.json` — 英文资源(9 个)
- `web/src/i18n/locales/zh/{common,nav,home,prompts,transfer,devices,scratchpad,welcome,settings}.json` — 中文资源(9 个)
- `web/src/hooks/useLanguage.ts` — 语言 hook(复刻 useTheme)
- `web/src/components/layout/LanguageSwitcher/{LanguageSwitcher.tsx, LanguageSwitcher.module.css, index.ts}` — 切换器组件

**修改:**
- `web/package.json` — 加 i18next / react-i18next 依赖
- `web/src/main.tsx` — 引入 `./i18n` 副作用初始化
- `web/src/components/layout/AppShell/AppShell.tsx` — footer 接入 LanguageSwitcher + nav label 改 t()
- `web/src/components/primitives/StatusDot/StatusDot.tsx` — statusLabels 改 t()
- `web/src/components/domain/TransferItem/TransferItem.tsx` — STATUS_LABEL/方向/按钮文案改 t()
- `web/src/lib/format.ts` — formatRelativeTime 改 Intl + 动态 locale
- `web/src/pages/{Welcome,Home,Prompts,Transfer,Devices,Scratchpad,Settings}/*/index.tsx` — 提取文案为 t()
- `web/CLAUDE.md` — 记录 i18n 约定与 useLanguage

**命名约定(贯穿全计划,所有 subagent 必须遵守):**
- key 风格:camelCase(如 `activeCount`、`deviceName`)
- 插值:`{{var}}`(如 `"active {{n}}"`)
- 复数:en 用 `{key}` + `{key}_other`,zh 只用 `{key}`
- common 子结构:
  - `action.{copy,delete,save,cancel,confirm,refresh,new,edit,search,open,retry,continue,pause,resume}`
  - `status.device.{online,offline,busy,away}`
  - `status.transfer.{pending,transferring,completed,failed,cancelled}`
  - `direction.{send,receive}`
- 技术术语保留英文:Prompt、GitHub、GitHub Releases、KB/MB/GB、SHA256 在 en/zh 资源里**都保留英文**

---

## Task 1: 安装依赖 + i18n 初始化基建

**Files:**
- Modify: `web/package.json`
- Create: `web/src/i18n/index.ts`
- Create: `web/src/i18n/locales/en/common.json`,`web/src/i18n/locales/zh/common.json`(本任务只建最小骨架,后续 Task 4 填充)
- Modify: `web/src/main.tsx`

- [ ] **Step 1: 安装 i18next 与 react-i18next**

```bash
cd web && npm install i18next react-i18next
```

预期:`package.json` 的 `dependencies` 出现 `i18next`、`react-i18next`。

- [ ] **Step 2: 建最小 common 资源骨架(仅占位,保证 import 不报错)**

`web/src/i18n/locales/en/common.json`:
```json
{
  "app": { "name": "Claude Partner" }
}
```

`web/src/i18n/locales/zh/common.json`:
```json
{
  "app": { "name": "Claude Partner" }
}
```

- [ ] **Step 3: 创建 i18n 初始化文件**

`web/src/i18n/index.ts`:
```ts
/**
 * i18next 初始化
 *
 * Business Logic（为什么需要这个文件）:
 *   应用需要中英文双语切换;语言偏好存 localStorage,首次按系统
 *   语言推断(navigator.language 以 zh 开头→中文),其余回退英文。
 *
 * Code Logic（这个文件做什么）:
 *   - 同步 import 全部 namespace 的 en/zh JSON 资源
 *   - detectLanguage:localStorage['cp-lang'] > navigator.language > 'en'
 *   - 配置 fallbackLng='en'、defaultNS='common'
 *   - declare module 让 react-i18next 的 t() 在编译期校验 key
 */
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';

import enCommon from './locales/en/common.json';
import enNav from './locales/en/nav.json';
import enHome from './locales/en/home.json';
import enPrompts from './locales/en/prompts.json';
import enTransfer from './locales/en/transfer.json';
import enDevices from './locales/en/devices.json';
import enScratchpad from './locales/en/scratchpad.json';
import enWelcome from './locales/en/welcome.json';
import enSettings from './locales/en/settings.json';

import zhCommon from './locales/zh/common.json';
import zhNav from './locales/zh/nav.json';
import zhHome from './locales/zh/home.json';
import zhPrompts from './locales/zh/prompts.json';
import zhTransfer from './locales/zh/transfer.json';
import zhDevices from './locales/zh/devices.json';
import zhScratchpad from './locales/zh/scratchpad.json';
import zhWelcome from './locales/zh/welcome.json';
import zhSettings from './locales/zh/settings.json';

export type AppLanguage = 'en' | 'zh';
export const LANGUAGE_STORAGE_KEY = 'cp-lang';

/** 检测初始语言:localStorage > 系统语言 > en */
export function detectLanguage(): AppLanguage {
  if (typeof window === 'undefined') return 'en';
  const stored = window.localStorage.getItem(LANGUAGE_STORAGE_KEY);
  if (stored === 'en' || stored === 'zh') return stored;
  const nav = window.navigator.language?.toLowerCase() ?? '';
  return nav.startsWith('zh') ? 'zh' : 'en';
}

export const resources = {
  en: {
    common: enCommon,
    nav: enNav,
    home: enHome,
    prompts: enPrompts,
    transfer: enTransfer,
    devices: enDevices,
    scratchpad: enScratchpad,
    welcome: enWelcome,
    settings: enSettings,
  },
  zh: {
    common: zhCommon,
    nav: zhNav,
    home: zhHome,
    prompts: zhPrompts,
    transfer: zhTransfer,
    devices: zhDevices,
    scratchpad: zhScratchpad,
    welcome: zhWelcome,
    settings: zhSettings,
  },
} as const;

// 让 t('common:xxx') 在编译期校验 key,拼错即 tsc 报错
declare module 'i18next' {
  interface CustomTypeOptions {
    defaultNS: 'common';
    resources: (typeof resources)['en'];
  }
}

void i18n.use(initReactI18next).init({
  resources,
  lng: detectLanguage(),
  fallbackLng: 'en',
  defaultNS: 'common',
  interpolation: {
    escapeValue: false, // React 已转义,无需再 escape
  },
});

export default i18n;
```

注意:`index.ts` 此时 import 了尚不存在的 nav/home/... 等 8 个 JSON。下一步先建空骨架避免 tsc 报错。

- [ ] **Step 4: 建 8 个空资源骨架(供 import 解析)**

为 `en` 与 `zh` 各创建以下 8 个文件,内容均为 `{}`:
`nav.json`、`home.json`、`prompts.json`、`transfer.json`、`devices.json`、`scratchpad.json`、`welcome.json`、`settings.json`。

(后续 Task 4 / Task 7-13 会填充真实内容。)

- [ ] **Step 5: main.tsx 接入 i18n 副作用初始化**

`web/src/main.tsx`,在现有样式 import 之后、`createRoot` 之前加一行:

```ts
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import { BrowserRouter } from 'react-router-dom';
import App from './App';

import './styles/tokens.css';
import './styles/reset.css';
import './styles/globals.css';
import './i18n'; // i18next 初始化(副作用导入,必须在 render 前)

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <BrowserRouter
      future={{
        v7_startTransition: true,
        v7_relativeSplatPath: true,
      }}
    >
      <App />
    </BrowserRouter>
  </StrictMode>
);
```

- [ ] **Step 6: 验证构建通过**

```bash
cd web && npm run build
```

预期:`tsc -b` 与 `vite build` 均无错误。

- [ ] **Step 7: 提交**

```bash
git add web/package.json web/package-lock.json web/src/i18n web/src/main.tsx
git commit -m "feat(i18n): 引入 react-i18next 基建与初始化"
```

---

## Task 2: useLanguage hook

**Files:**
- Create: `web/src/hooks/useLanguage.ts`

- [ ] **Step 1: 创建 useLanguage(完全复刻 useTheme 范式)**

`web/src/hooks/useLanguage.ts`:
```ts
/**
 * useLanguage Hook
 *
 * Business Logic（为什么需要这个 hook）:
 *   应用需要中英文切换并在所有组件、标签页、多个 QWebEngineView 窗口
 *   之间即时同步。语言偏好持久化到 localStorage,复用 useTheme 的事件
 *   同步范式。
 *
 * Code Logic（这个 hook 做什么）:
 *   - 暴露当前 language('en'|'zh')、setLanguage、toggleLanguage
 *   - setLanguage:i18next.changeLanguage + 写 localStorage + 派发
 *     'cp-lang-change' 自定义事件
 *   - 监听 'cp-lang-change' 与 'storage' 事件,跨标签页/窗口同步
 */
import { useCallback, useEffect, useState } from 'react';
import i18n, { LANGUAGE_STORAGE_KEY, type AppLanguage } from '../i18n';

export const LANGUAGE_CHANGE_EVENT = 'cp-lang-change';

function readInitialLanguage(): AppLanguage {
  return i18n.language === 'zh' ? 'zh' : 'en';
}

export interface UseLanguageResult {
  language: AppLanguage;
  setLanguage: (next: AppLanguage) => void;
  toggleLanguage: () => void;
}

export function useLanguage(): UseLanguageResult {
  const [language, setLanguageState] = useState<AppLanguage>(readInitialLanguage);

  // 监听其他实例派发的切换事件 + 跨标签 storage 事件
  useEffect(() => {
    const changeHandler = (e: Event) => {
      const ce = e as CustomEvent<AppLanguage>;
      if (ce.detail === 'en' || ce.detail === 'zh') {
        setLanguageState(ce.detail);
      }
    };
    const storageHandler = (e: StorageEvent) => {
      if (
        e.key === LANGUAGE_STORAGE_KEY &&
        (e.newValue === 'en' || e.newValue === 'zh')
      ) {
        void i18n.changeLanguage(e.newValue);
        setLanguageState(e.newValue);
      }
    };
    window.addEventListener(LANGUAGE_CHANGE_EVENT, changeHandler);
    window.addEventListener('storage', storageHandler);
    return () => {
      window.removeEventListener(LANGUAGE_CHANGE_EVENT, changeHandler);
      window.removeEventListener('storage', storageHandler);
    };
  }, []);

  const setLanguage = useCallback((next: AppLanguage) => {
    setLanguageState(next);
    void i18n.changeLanguage(next);
    if (typeof window !== 'undefined') {
      window.localStorage.setItem(LANGUAGE_STORAGE_KEY, next);
      window.dispatchEvent(
        new CustomEvent<AppLanguage>(LANGUAGE_CHANGE_EVENT, { detail: next })
      );
    }
  }, []);

  const toggleLanguage = useCallback(() => {
    setLanguage(language === 'zh' ? 'en' : 'zh');
  }, [language, setLanguage]);

  return { language, setLanguage, toggleLanguage };
}
```

- [ ] **Step 2: 验证**

```bash
cd web && npm run lint && npm run build
```

预期:无错误。

- [ ] **Step 3: 提交**

```bash
git add web/src/hooks/useLanguage.ts
git commit -m "feat(i18n): 新增 useLanguage hook(复刻 useTheme 同步范式)"
```

---

## Task 3: LanguageSwitcher 组件 + AppShell 接入

**Files:**
- Create: `web/src/components/layout/LanguageSwitcher/LanguageSwitcher.tsx`
- Create: `web/src/components/layout/LanguageSwitcher/LanguageSwitcher.module.css`
- Create: `web/src/components/layout/LanguageSwitcher/index.ts`
- Modify: `web/src/components/layout/AppShell/AppShell.tsx`

- [ ] **Step 1: 创建 LanguageSwitcher 组件**

`web/src/components/layout/LanguageSwitcher/LanguageSwitcher.tsx`:
```tsx
/**
 * LanguageSwitcher（中英文语言切换器）
 *
 * Business Logic（为什么需要这个组件）:
 *   Sidebar 底部提供全局语言切换入口,让用户在任意页面随手切换中英文。
 *
 * Code Logic（这个组件做什么）:
 *   - useLanguage() 获取 language / setLanguage
 *   - 渲染紧凑的 EN / 中 两段式切换器,高亮当前语言
 */
import { useLanguage } from '../../../hooks/useLanguage';
import type { AppLanguage } from '../../../i18n';
import styles from './LanguageSwitcher.module.css';

export interface LanguageSwitcherProps {
  className?: string;
}

const OPTIONS: ReadonlyArray<{ value: AppLanguage; label: string }> = [
  { value: 'en', label: 'EN' },
  { value: 'zh', label: '中' },
];

export function LanguageSwitcher({ className }: LanguageSwitcherProps) {
  const { language, setLanguage } = useLanguage();
  const cls = [styles.switcher, className].filter(Boolean).join(' ');

  return (
    <div className={cls} role="group" aria-label="language">
      {OPTIONS.map((opt) => {
        const active = language === opt.value;
        return (
          <button
            key={opt.value}
            type="button"
            className={active ? styles.optionActive : styles.option}
            onClick={() => setLanguage(opt.value)}
            aria-pressed={active}
          >
            {opt.label}
          </button>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 2: 创建样式(参考 ThemeToggle 的 26px 圆形紧凑风格)**

`web/src/components/layout/LanguageSwitcher/LanguageSwitcher.module.css`:
```css
.switcher {
  display: inline-flex;
  align-items: center;
  gap: 2px;
  padding: 2px;
  border-radius: 999px;
  background: var(--surface-warm, rgba(0, 0, 0, 0.04));
}

.option,
.optionActive {
  border: none;
  background: transparent;
  color: var(--text-meta, #888);
  font-size: 11px;
  font-weight: 600;
  line-height: 1;
  padding: 4px 8px;
  border-radius: 999px;
  cursor: pointer;
  transition: background 0.15s, color 0.15s;
}

.option:hover {
  color: var(--text, #333);
}

.optionActive {
  background: var(--accent, #2563eb);
  color: #fff;
}
```

- [ ] **Step 3: 创建 barrel**

`web/src/components/layout/LanguageSwitcher/index.ts`:
```ts
export { LanguageSwitcher } from './LanguageSwitcher';
export type { LanguageSwitcherProps } from './LanguageSwitcher';
```

- [ ] **Step 4: AppShell footer 接入 LanguageSwitcher**

`web/src/components/layout/AppShell/AppShell.tsx` 修改点:
1. import 区加 `import { LanguageSwitcher } from '../LanguageSwitcher';`
2. footer 的 `footerToggle` 内,在 `<ThemeToggle />` 旁加 `<LanguageSwitcher />`

修改后 footer 段:
```tsx
<div className={styles.footerToggle}>
  <LanguageSwitcher />
  <ThemeToggle />
</div>
```

(nav 的 label 改造放到 Task 4,本步只接切换器。)

- [ ] **Step 5: 验证 + dev 抽查**

```bash
cd web && npm run lint && npm run build
```

预期:无错误。`npm run dev` 启动后(需后端运行),Sidebar 底部应出现 EN / 中 切换器。

- [ ] **Step 6: 提交**

```bash
git add web/src/components/layout/LanguageSwitcher web/src/components/layout/AppShell/AppShell.tsx
git commit -m "feat(i18n): LanguageSwitcher 组件并接入 Sidebar 底部"
```

---

## Task 4: common + nav 资源填充

**Files:**
- Modify: `web/src/i18n/locales/en/common.json`、`web/src/i18n/locales/zh/common.json`
- Modify: `web/src/i18n/locales/en/nav.json`、`web/src/i18n/locales/zh/nav.json`
- Modify: `web/src/components/layout/AppShell/AppShell.tsx`(nav label 改 t)

- [ ] **Step 1: 填充 common.json(动作 / 状态枚举 / 方向)**

`web/src/i18n/locales/en/common.json`:
```json
{
  "app": { "name": "Claude Partner" },
  "action": {
    "copy": "Copy",
    "delete": "Delete",
    "save": "Save",
    "cancel": "Cancel",
    "confirm": "Confirm",
    "refresh": "Refresh",
    "new": "New",
    "edit": "Edit",
    "search": "Search",
    "open": "Open",
    "retry": "Retry",
    "continue": "Continue",
    "pause": "Pause",
    "resume": "Resume"
  },
  "status": {
    "device": {
      "online": "Online",
      "offline": "Offline",
      "busy": "Busy",
      "away": "Away"
    },
    "transfer": {
      "pending": "Pending",
      "transferring": "Transferring",
      "completed": "Completed",
      "failed": "Failed",
      "cancelled": "Cancelled"
    }
  },
  "direction": {
    "send": "Sending to peer",
    "receive": "Receiving from peer"
  },
  "empty": {
    "noResults": "No results"
  }
}
```

`web/src/i18n/locales/zh/common.json`:
```json
{
  "app": { "name": "Claude Partner" },
  "action": {
    "copy": "复制",
    "delete": "删除",
    "save": "保存",
    "cancel": "取消",
    "confirm": "确认",
    "refresh": "刷新",
    "new": "新建",
    "edit": "编辑",
    "search": "搜索",
    "open": "打开",
    "retry": "重试",
    "continue": "继续",
    "pause": "暂停",
    "resume": "继续"
  },
  "status": {
    "device": {
      "online": "在线",
      "offline": "离线",
      "busy": "忙碌",
      "away": "离开"
    },
    "transfer": {
      "pending": "等待中",
      "transferring": "传输中",
      "completed": "已完成",
      "failed": "失败",
      "cancelled": "已取消"
    }
  },
  "direction": {
    "send": "发送至对端",
    "receive": "接收自对端"
  },
  "empty": {
    "noResults": "暂无结果"
  }
}
```

- [ ] **Step 2: 填充 nav.json**

`web/src/i18n/locales/en/nav.json`:
```json
{
  "home": "Home",
  "transfer": "Transfer",
  "prompts": "Prompts",
  "scratchpad": "Scratchpad",
  "devices": "Devices",
  "settings": "Settings"
}
```

`web/src/i18n/locales/zh/nav.json`:
```json
{
  "home": "首页",
  "transfer": "文件传输",
  "prompts": "Prompt 库",
  "scratchpad": "速记本",
  "devices": "设备",
  "settings": "设置"
}
```

- [ ] **Step 3: AppShell nav label 改用 t()**

`web/src/components/layout/AppShell/AppShell.tsx`:
1. 顶部加 `import { useTranslation } from 'react-i18next';`
2. 组件内(版本号 `useAppVersion()` 调用附近,注意 hooks 必须在所有 early return 之前——本组件无 early return,直接加)加:
```tsx
const { t } = useTranslation();
```
3. nav 区改为:
```tsx
<nav className={styles.navList} aria-label="primary">
  <NavItem to="/" label={t('nav:home')} icon={<HomeIcon />} />
  <NavItem to="/transfer" label={t('nav:transfer')} icon={<TransferIcon />} />
  <NavItem to="/prompts" label={t('nav:prompts')} icon={<PromptsIcon />} />
  <NavItem to="/scratchpad" label={t('nav:scratchpad')} icon={<ScratchpadIcon />} />
  <NavItem to="/devices" label={t('nav:devices')} icon={<DevicesIcon />} />
  <NavItem to="/settings" label={t('nav:settings')} icon={<SettingsIcon />} />
</nav>
```

(nav 的 `aria-label="primary"` 是无障碍语义标识,保留英文即可,不必翻译。)

- [ ] **Step 4: 验证**

```bash
cd web && npm run build
```

预期:tsc 通过(类型扩展校验 `nav:*`、`common:*` key 存在)。

- [ ] **Step 5: 提交**

```bash
git add web/src/i18n/locales/en/common.json web/src/i18n/locales/zh/common.json web/src/i18n/locales/en/nav.json web/src/i18n/locales/zh/nav.json web/src/components/layout/AppShell/AppShell.tsx
git commit -m "feat(i18n): 填充 common/nav 资源,AppShell 导航改 t()"
```

---

## Task 5: 状态枚举组件改造(StatusDot + TransferItem)

**Files:**
- Modify: `web/src/components/primitives/StatusDot/StatusDot.tsx`
- Modify: `web/src/components/domain/TransferItem/TransferItem.tsx`

- [ ] **Step 1: StatusDot 用 t() 替换 statusLabels**

`web/src/components/primitives/StatusDot/StatusDot.tsx`:
1. 加 import:`import { useTranslation } from 'react-i18next';`
2. 删除 `const statusLabels: Record<StatusDotStatus, string> = {...}` 整块
3. 组件函数体内(`const { status, ... } = props;` 之后)加:
```tsx
const { t } = useTranslation();
const label = t(`common:status.device.${status}`);
```
4. `aria-label={statusLabels[status]}` 改为 `aria-label={label}`

- [ ] **Step 2: TransferItem 用 t() 替换 STATUS_LABEL / 方向 / 按钮文案**

`web/src/components/domain/TransferItem/TransferItem.tsx`:
1. 加 import:`import { useTranslation } from 'react-i18next';`
2. 删除 `const STATUS_LABEL = {...}` 整块(STATUS_TONE / STATUS_BG / STATUS_BORDER 保留,它们是样式映射不翻译)
3. `TransferItemInner` 函数体内开头加 `const { t } = useTranslation();`
4. `const label = STATUS_LABEL[task.status];` 改为 `const label = t(\`common:status.transfer.${task.status}\`);`
5. 对端文案(约 153 行):
```tsx
{task.peerDevice ?? t(`common:direction.${task.direction}`)}
```
6. 按钮文案改为(逐个替换现有中文字面量):
   - Pause 按钮:`aria-label={t('common:action.pause')}` `title={t('common:action.pause')}`
   - Cancel 按钮:`aria-label={t('common:action.cancel')}` `title={t('common:action.cancel')}`
   - Retry 按钮:`aria-label={t('common:action.retry')}` `title={t('common:action.retry')}`,子文本 `重试` → `{t('common:action.retry')}`
   - Continue 按钮:`aria-label={t('common:action.continue')}` `title={t('common:action.continue')}`,子文本 `继续` → `{t('common:action.continue')}`
   - Open 按钮:`aria-label={t('common:action.open')}` `title={t('common:action.open')}`

注意:TransferItem 内部的 `formatBytes` / `formatSpeed`(英文单位)本任务**不动**(它们与 lib/format.ts 逻辑有差异,合并会改变显示精度,超出 i18n 范围)。

- [ ] **Step 3: 验证**

```bash
cd web && npm run lint && npm run build
```

预期:tsc 通过(`common:status.*`、`common:direction.*`、`common:action.*` key 已在 Task 4 定义)。

- [ ] **Step 4: 提交**

```bash
git add web/src/components/primitives/StatusDot/StatusDot.tsx web/src/components/domain/TransferItem/TransferItem.tsx
git commit -m "feat(i18n): StatusDot/TransferItem 状态枚举与按钮文案改 t()"
```

---

## Task 6: lib/format.ts 改造

**Files:**
- Modify: `web/src/lib/format.ts`

- [ ] **Step 1: formatRelativeTime 改用 Intl.RelativeTimeFormat + 动态 locale**

`web/src/lib/format.ts` 顶部加 import:
```ts
import i18n, { type AppLanguage } from '../i18n';
```

替换 `formatRelativeTime` 函数为:
```ts
/**
 * 格式化时间戳为相对时间(随当前语言切换 en/zh)
 */
export function formatRelativeTime(
  iso: string,
  lang: AppLanguage = i18n.language === 'zh' ? 'zh' : 'en'
): string {
  const date = new Date(iso);
  const diffMs = Date.now() - date.getTime();
  const locale = lang === 'zh' ? 'zh-CN' : 'en-US';
  const rtf = new Intl.RelativeTimeFormat(locale, { numeric: 'auto' });
  const sec = Math.round(diffMs / 1000);
  const min = Math.round(sec / 60);
  const hr = Math.round(min / 60);
  const day = Math.round(hr / 24);
  if (sec < 60) return rtf.format(-sec, 'second');
  if (min < 60) return rtf.format(-min, 'minute');
  if (hr < 24) return rtf.format(-hr, 'hour');
  if (day < 7) return rtf.format(-day, 'day');
  return date.toLocaleDateString(locale);
}
```

效果:`numeric: 'auto'` 下,英文输出 "3 minutes ago" / "just now",中文输出 "3 分钟前" / "刚刚"。

(formatBytes / formatSpeed 用英文单位,不改。)

- [ ] **Step 2: 验证**

```bash
cd web && npm run lint && npm run build
```

预期:无错误。

- [ ] **Step 3: 提交**

```bash
git add web/src/lib/format.ts
git commit -m "feat(i18n): formatRelativeTime 改用 Intl.RelativeTimeFormat + 动态 locale"
```

---

## Task 7-13: 页面文案改造(可并行,每页一个 subagent)

> **统一改造规范(每个页面 task 都遵守):**
> 1. 在页面组件顶部 `import { useTranslation } from 'react-i18next';`,组件函数体内 hooks 区(所有 early return **之前**)加 `const { t } = useTranslation();`
> 2. 扫描该页 `*.tsx` 所有硬编码中文字面量(JSX 文本、属性值、placeholder、title、aria-label、toast/通知、确认对话框文案),提取到对应 ns 的 en/zh 资源
> 3. **hooks 顺序铁律**(用户全局规则 20):`useTranslation` 必须在所有 `if (...) return ...` 早返回之前调用
> 4. key 用 camelCase;动态值用插值 `{{var}}`;英文需要复数时配 `{key}_other`
> 5. 技术术语(Prompt、GitHub、KB/MB/GB)在 en/zh 都保留英文
> 6. 改完跑 `cd web && npm run lint && npm run build`,tsc 会校验所有 `t('该ns:xxx')` 的 key 是否存在于资源文件
> 7. 提交:`git commit -m "feat(i18n): <页面名> 页文案国际化"`

### Task 7: Welcome 页

**Files:** `web/src/pages/Welcome/`(主组件 + 子文件),`web/src/i18n/locales/{en,zh}/welcome.json`

代表文案点(须全量扫描补全):
- 标题:"欢迎使用 Claude Partner" → en `"Welcome to Claude Partner"` / zh `"欢迎使用 Claude Partner"`
- 权限项:"屏幕录制"、"输入监控"、"通知权限"、"可访问性"
- 状态:"正在检查权限状态…"、"权限已就绪"
- 按钮:"暂时跳过"、"继续使用"

资源骨架(`welcome.json`):
```json
{ "title": "...", "subtitle": "...", "checkingPermission": "...", "permissionReady": "...", "skip": "...", "continue": "...", "permission": { "screenRecording": "...", "inputMonitoring": "...", "notifications": "...", "accessibility": "..." } }
```

- [ ] 扫描 Welcome/*.tsx 全量提取中文 → 填 welcome.json(en/zh)
- [ ] 组件改 t('welcome:*')
- [ ] `npm run lint && npm run build` 通过
- [ ] 提交

### Task 8: Home 页

**Files:** `web/src/pages/Home/`,`web/src/i18n/locales/{en,zh}/home.json`

代表文案点 + **动态/复数**:
- "Prompt 管理" / "局域网" / "本机"
- **复数**:`"{n} 台在线"` → en `"{{count}} device online"` + `"{{count}} devices online_other"` / zh `"{{count}} 台在线"`,调用 `t('home:devicesOnline', { count: n })`
- "新建 Prompt"、"发送文件"、"最近的 Prompts"、"暂无 Prompt"、"未连接"、"现在"
- 相对时间复用 `formatRelativeTime`(Task 6 已随语言切换,无需改)

资源骨架(`home.json`),注意复数双键:
```json
{ "title": "...", "local": "...", "lan": "...", "devicesOnline": "{{count}} device online", "devicesOnline_other": "{{count}} devices online", "disconnected": "...", "newPrompt": "...", "sendFile": "...", "recentPrompts": "...", "emptyPrompt": "..." }
```

- [ ] 扫描 Home/*.tsx 全量提取 → 填 home.json(注意复数双键)
- [ ] 组件改 t('home:*'),`{count}` 插值
- [ ] `npm run lint && npm run build` 通过
- [ ] 提交

### Task 9: Prompts 页

**Files:** `web/src/pages/Prompts/`,`web/src/i18n/locales/{en,zh}/prompts.json`

代表文案点:
- "Prompt 库"、"精心策划的提示词集合…"(en 可保留 "Prompt library")
- 按钮:"同步"、"新建"
- 空态:"尚未添加 Prompt"
- 确认对话框:"删除 Prompt?"、"该操作不可撤销…"
- 标签筛选、搜索 placeholder

资源骨架(`prompts.json`):
```json
{ "title": "...", "subtitle": "...", "sync": "...", "new": "...", "empty": "...", "deleteTitle": "...", "deleteConfirm": "...", "searchPlaceholder": "..." }
```

- [ ] 扫描 Prompts/*.tsx 全量提取 → 填 prompts.json
- [ ] 组件改 t('prompts:*')
- [ ] `npm run lint && npm run build` 通过
- [ ] 提交

### Task 10: Transfer 页

**Files:** `web/src/pages/Transfer/`,`web/src/i18n/locales/{en,zh}/transfer.json`

代表文案点 + **动态**:
- "文件传输"、"选择目标设备"
- **插值**:`"发送「{{file}}」"`、`"活跃 {{n}}"`、`"已完成 {{n}}"`、`"失败 {{n}}"`
- "拖拽文件到此处 或 点击选择"、"支持任意大小 · 自动分块 1MB"
- "暂无传输任务"

资源骨架(`transfer.json`):
```json
{ "title": "...", "selectDevice": "...", "sendFile": "Send \"{{file}}\"", "active": "Active {{n}}", "completed": "Completed {{n}}", "failed": "Failed {{n}}", "dropHint": "...", "chunkHint": "...", "empty": "..." }
```

- [ ] 扫描 Transfer/*.tsx 全量提取 → 填 transfer.json
- [ ] 组件改 t('transfer:*'),插值 `{{file}}` / `{{n}}`
- [ ] `npm run lint && npm run build` 通过
- [ ] 提交

### Task 11: Devices 页

**Files:** `web/src/pages/Devices/`,`web/src/i18n/locales/{en,zh}/devices.json`

代表文案点 + **动态/复数**:
- "在线设备"、**复数**`"{n} 个在线"` → en `"{{count}} online"` + `_other` / zh `"{{count}} 个在线"`
- "局域网内通过 mDNS 自动发现的对端实例"
- "本机信息"、"刷新"、搜索 placeholder、"在线"/"离线"
- DeviceCard 的 `formatLastSeen`("最后活跃 X 秒/分钟/小时前")→ 复用 Task 6 的 `formatRelativeTime`(若 DeviceCard 内有独立实现,改为调用 lib/format.ts,删除重复——规则 9 复用)

资源骨架(`devices.json`):
```json
{ "title": "...", "onlineCount": "{{count}} online", "onlineCount_other": "{{count}} online", "desc": "...", "localInfo": "...", "refresh": "...", "searchPlaceholder": "...", "online": "...", "offline": "..." }
```

- [ ] 扫描 Devices/*.tsx + DeviceCard/*.tsx 全量提取 → 填 devices.json
- [ ] DeviceCard 重复的相对时间函数改用 lib/format.ts 的 formatRelativeTime(删除本地重复实现)
- [ ] 组件改 t('devices:*')
- [ ] `npm run lint && npm run build` 通过
- [ ] 提交

### Task 12: Scratchpad 页

**Files:** `web/src/pages/Scratchpad/`,`web/src/i18n/locales/{en,zh}/scratchpad.json`

代表文案点 + **动态**:
- "速记本"、"临时记录你的想法,内容不会保存…"
- "复制全部"、"清空"、**插值**`"{{n}} 字"`(en 用 `"{{n}} chars"`)
- 确认:"确认清空?"

资源骨架(`scratchpad.json`):
```json
{ "title": "...", "desc": "...", "copyAll": "...", "clear": "...", "charCount": "{{n}} chars", "clearConfirm": "..." }
```

- [ ] 扫描 Scratchpad/*.tsx 全量提取 → 填 scratchpad.json
- [ ] 组件改 t('scratchpad:*')
- [ ] `npm run lint && npm run build` 通过
- [ ] 提交

### Task 13: Settings 页(文案最多)

**Files:** `web/src/pages/Settings/`,`web/src/i18n/locales/{en,zh}/settings.json`

代表文案点(文案最密集,须仔细全量扫描):
- 卡片标题:"设置"、"基本设置"、"快捷键"、"同步与存储"、"关于"
- 副标题:"管理设备名、快捷键、同步策略与版本信息"
- 字段:"设备名称"、"接收目录"、"截图快捷键" 等
- 同步开关:"自动同步"、"保存传输历史"、"加密敏感 Prompt"
- 更新流:**插值**`"发现新版本 v{{version}}"`、`"下载更新 ({{size}})"`、"检查更新"、"安装并重启"、"已是最新版本"、`"已保存于 {{time}}"`
- 按钮:"保存"、"恢复默认"
- 快捷键项的说明文案

注意:版本号本身来自 `useAppVersion()`(后端 `/api/version`),**不要翻译版本号**,只翻译周围文案。

资源骨架(`settings.json`):
```json
{ "title": "...", "subtitle": "...", "basic": { "title": "...", "deviceName": "...", "receiveDir": "..." }, "shortcut": { "title": "...", "screenshot": "...", "...": "..." }, "sync": { "title": "...", "autoSync": "...", "saveHistory": "...", "encryptSensitive": "..." }, "about": { "title": "...", "version": "...", "checkUpdate": "...", "newVersionFound": "New version v{{version}} found", "downloadUpdate": "Download update ({{size}})", "installAndRestart": "...", "upToDate": "...", "savedAt": "Saved at {{time}}" }, "action": { "save": "...", "resetDefault": "..." } }
```

- [ ] 扫描 Settings/*.tsx 全量提取 → 填 settings.json(文案最多,逐项不遗漏)
- [ ] 组件改 t('settings:*'),版本号插值 `{{version}}`
- [ ] `npm run lint && npm run build` 通过
- [ ] 提交

---

## Task 14: 全量验证 + 文档更新

**Files:**
- Modify: `web/CLAUDE.md`

- [ ] **Step 1: 全量 lint + build**

```bash
cd web && npm run lint && npm run build
```

预期:全部通过,tsc 无未定义 key 报错。

- [ ] **Step 2: dev 抽查中英文切换**

`npm run dev`(需后端运行),验证:
- 首次访问按系统语言显示(中文系统→中文)
- 点 Sidebar 底部 EN / 中 切换,全站文案立即变化
- 刷新后语言保持(localStorage 生效)
- 相对时间("3 分钟前" / "3 minutes ago")随语言切换

- [ ] **Step 3: 更新 web/CLAUDE.md**

在 `## 架构` 的 hooks 条目下补充 `useLanguage`;并在架构区新增 i18n 说明条目,例如:

```markdown
- **i18n**: `src/i18n/` — react-i18next 多 namespace(en/zh),语言检测 localStorage > navigator.language > en;`useLanguage` 复刻 `useTheme` 同步范式;切换器在 Sidebar 底部。新增页面文案一律走 `src/i18n/locales/{en,zh}/<ns>.json` + `t('<ns>:<key>')`,**禁止在组件里硬编码用户可见中/英文字面量**。
```

- [ ] **Step 4: 提交**

```bash
git add web/CLAUDE.md
git commit -m "docs(i18n): 更新 web/CLAUDE.md 记录 i18n 约定与 useLanguage"
```

---

## 完成标准

- `npm run lint` 与 `npm run build` 全绿(tsc 类型扩展保证无拼错 key)
- Sidebar 底部 LanguageSwitcher 可切换中英文,全站文案、相对时间、状态枚举随之变化
- 首次跟随系统语言,切换后刷新保持
- `web/CLAUDE.md` 已记录 i18n 约定
- DesignSystem 页保持英文(本计划不涉及)
